# Optional: use this instead of entering a key for each app session.
# Copy to .env, put your key there, and restart the local server.
# .env is ignored by Git and is never served to the browser.
OPENAI_API_KEY=
